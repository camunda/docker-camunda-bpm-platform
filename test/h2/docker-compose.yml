version: '3'

services:
    camunda:
        image: camunda/camunda-bpm-platform:${DISTRO:-latest}
        environment:
            - JAVA_OPTS=-Djava.security.egd=file:/dev/./urandom
        ports:
            - "8080:8080"
