language: generic

os: linux

services:
  - docker

env:
  - DISTRO=tomcat
#   - DISTRO=tomcat EE=true
#   - DISTRO=wildfly
#   - DISTRO=wildfly EE=true
#   - DISTRO=run
#   - DISTRO=run EE=true

jobs:
  include:
    - if: repo = "maxdanilov-test/docker-camunda-bpm-platform"
      env: DISTRO=tomkitty

script:
  - ./pipeline.sh

