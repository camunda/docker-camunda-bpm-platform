language: generic

sudo: required

services:
    - docker

script:
    - docker build -t camunda/camunda-bpm-platform:ce .
    - docker build -t camunda/camunda-bpm-platform:ee --build-arg EE=true --build-arg USER=${NEXUS_USER} --build-arg PASSWORD=${NEXUS_PASS} --build-arg DISTRO=wildfly10 .
